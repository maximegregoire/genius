import skflow
import numpy as np
from networks import network
from sklearn import datasets, metrics

#iris = datasets.load_iris()
new_model = network.Network("test123", 'upload/training.csv', 'upload/testing.csv')
new_model.delimitation = ','
new_model.separate = True
new_model.input_start_column = 0
new_model.input_end_column = 37
new_model.output_column = 38
new_model.qualitative_outputs = qualitative
new_model.extractData()


#classifier = skflow.TensorFlowDNNClassifier(hidden_units=[10, 20, 10], n_classes=2)
#classifier.fit(iris.data, iris.target)
#score = metrics.accuracy_score(classifier.predict(iris.data), iris.target)
#print("Accuracy: %f" % score)
